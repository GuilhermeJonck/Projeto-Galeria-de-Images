function toggleMode() {
    const html = document.documentElement
    html.classList.toggle("light")
    //substituir a image
}
  
lightbox.option({
    resizeDuration: 200,
    wrapAround: true,
    disableScrolling: true,
})

